

    describe('Tester addition', () => {
        it('Test d\'addition', () => {
            const a = 3;
            const b = 5;
            expect(a + b).toEqual(8);
        });
    });

    describe('Tester soustraction', () => {
        it('Test de soustraction', () => {
            const a = 3;
            const b = 5;
            expect(a - b).toEqual(-2);
        });
    });

    describe('Tester multiplication', () => {
        it('Test de multiplication', () => {
            const a = 3;
            const b = 5;
            expect(a * b).toEqual(15);
        });
    }); 

